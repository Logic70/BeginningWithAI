---
name: hello-world
description: 向你问好。当用户输入包含"你好"、"hello"等问候时激活。
---

# Hello World Skill

这是一个最简单的 Skill 示例，用于演示 Skills 的基本结构。

## 工作流程

1. 获取当前时间
2. 根据时间返回不同的问候语
3. 如果提供了名字，使用个性化问候

## 可用脚本

### hello.py - 问候脚本

```bash
python scripts/hello.py [名字]
```

参数：
- 可选的位置参数：名字（默认"朋友"）

示例输出：
```
当前时间: 2024-01-15 09:30
问候: 早上好，张三！
```

## 技术细节

- 使用 Python 的 `datetime` 模块获取当前时间
- 根据小时数判断时段（5-12点早上，12-18点下午，其他晚上）
- 脚本路径相对于 Skill 目录
